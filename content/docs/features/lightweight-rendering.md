+++
title = "Lightweight Rendering"
weight = 50
+++

# Lightweight Rendering

Galley draws only the PDF page itself — no annotation overlays, no editing
tools, no toolbars stacked over the canvas. As a result, page rendering
stays light even when you flip through pages quickly.

Combined with [Auto-Reload]({{< relref "auto-reload" >}}), this means you can
keep Galley open beside your editor and your build loop without ever
noticing the viewer is there.
